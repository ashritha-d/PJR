node_modules
.next
out
dist
*.log
.env
.env.local
.env*.local
prisma/dev.db
prisma/dev.db-journal
public/uploads/**
!public/uploads/.gitkeep
.DS_Store
Thumbs.db

# Raw source material (not part of the deployed app; kept locally only).
# The logo/photos actually used by the site already live under public/.
/images/
/*.pdf
.kilo/

.vercel
.env*
!.env.example

# Personal documents that have appeared at the project root (not app assets —
# never commit these regardless of filename/extension).
/*.jpeg
/*.jpg
/*.png
